<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="see_own_contact_rule" model="ir.rule">
        <field name="name">Own Contact</field>
        <field ref="model_res_partner" name="model_id"/>
        <field name="domain_force">['|','|',('user_id','=',user.id),('id', '=', user.partner_id.id),('user_id', '=', False)]</field>
        <field name="groups" eval="[(4, ref('sales_team.group_sale_salesman'))]"/>
    </record>

    <record id="see_all_contact_for_sale" model="ir.rule">
        <field name="name">All user sale manager</field>
        <field ref="model_res_partner" name="model_id"/>
        <field name="domain_force">[(1,'=', 1)]</field>
        <field name="groups" eval="[(4, ref('sales_team.group_sale_manager'))]"/>
    </record>

    <record id="rules_for_salesperson" model="ir.rule">
        <field name="name">Salesperson Record Rules</field>
        <field name="model_id" ref="sale.model_res_partner"/>
        <field name="domain_force">[('create_uid','=',user.id)]</field>
        <field name="groups" eval="[(4, ref('sales_team.group_sale_salesman'))]" />
    </record>

    <record id="individual_contacts_menu" model="ir.ui.menu">
        <field name="name">Individual Contacts</field>
        <field name="groups_id" eval="[(4, ref('sales_team.group_sale_manager'))]"/>
    </record>

     <record id="group_gcrm" model="res.groups">
            <field name="name">GCRM</field>
            <field name="implied_ids" eval="[(4, ref('sales_team.group_sale_salesman'))]"/>
        </record>


</odoo>
